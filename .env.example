# =============================================================================
# ZCC RTMS Zoom App - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# =============================================================================

# -----------------------------------------------------------------------------
# Zoom App Credentials (Get from Zoom Marketplace)
# -----------------------------------------------------------------------------
ZOOM_APP_CLIENT_ID=your_client_id_here
ZOOM_APP_CLIENT_SECRET=your_client_secret_here
ZOOM_SECRET_TOKEN=your_webhook_secret_token_here
ZOOM_BEARER_TOKEN=your_bearer_token_here

# -----------------------------------------------------------------------------
# Zoom Configuration
# -----------------------------------------------------------------------------
ZOOM_HOST=https://zoom.us

# -----------------------------------------------------------------------------
# Server Ports
# -----------------------------------------------------------------------------
# Frontend port (React dev server)
PORT=3000
# Backend API port
BACKEND_PORT=3001
# RTMS server port
RTMS_PORT=8080

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
NODE_ENV=development

# -----------------------------------------------------------------------------
# URLs (Update with your ngrok or production URLs)
# -----------------------------------------------------------------------------
# Public URL for backend (where Zoom sends webhooks)
# For ngrok: https://your-ngrok-id.ngrok-free.dev
PUBLIC_URL=https://your-ngrok-url.ngrok-free.dev

# Frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:3000

# RTMS server URL (internal communication)
RTMS_SERVER_URL=http://localhost:8080

# OAuth redirect URL (must match Marketplace configuration)
# For ngrok: https://your-ngrok-id.ngrok-free.dev/api/auth/callback
ZOOM_REDIRECT_URL=https://your-ngrok-url.ngrok-free.dev/api/auth/callback

# -----------------------------------------------------------------------------
# Frontend Configuration (React env vars must start with REACT_APP_)
# -----------------------------------------------------------------------------
REACT_APP_BACKEND_URL=http://localhost:3001
REACT_APP_ZOOM_CLIENT_ID=your_client_id_here

# -----------------------------------------------------------------------------
# Session Configuration
# -----------------------------------------------------------------------------
SESSION_SECRET=your_random_session_secret_change_in_production

# -----------------------------------------------------------------------------
# Redis Configuration (Optional - for production)
# -----------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379
REDIS_ENCRYPTION_KEY=your_32_byte_encryption_key_here

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Get credentials from Zoom Marketplace (Contact Center App)
# 3. Update ZOOM_APP_CLIENT_ID, ZOOM_APP_CLIENT_SECRET, ZOOM_SECRET_TOKEN, ZOOM_BEARER_TOKEN
# 4. Start ngrok: npm run ngrok (or: ngrok http 3001)
# 5. Update PUBLIC_URL and ZOOM_REDIRECT_URL with your ngrok URL
# 6. Update Zoom Marketplace with ngrok URLs (Webhook, OAuth, Home URL)
# 7. Run: npm start (starts all services with Docker)
# =============================================================================
